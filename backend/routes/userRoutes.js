import { Router } from "express";
// import { addToCart, getCart } from "../controllers/userController.js";


const userRoutes = Router();

// userRoutes.post("/add-to-cart", addToCart);
// userRoutes.get("/cart", getCart);

export default userRoutes;
